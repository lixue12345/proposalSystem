@model ProposalSystem.Models.FeedBack

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FeedBack</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Result, "Result1")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Result", String.Empty)
            @Html.ValidationMessageFor(model => model.Result)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ProposerOpinion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ProposerOpinion)
            @Html.ValidationMessageFor(model => model.ProposerOpinion)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Date)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Date)
            @Html.ValidationMessageFor(model => model.Date)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
