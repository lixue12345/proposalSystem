@model IEnumerable<ProposalSystem.Models.FeedBack>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Form</title>

</head>
<body>
    <table class="table table-bordered">
        <caption>用户评价</caption>
        <thead>
            <tr>
                <th>标题</th>
                <th>类型</th>
                <th>主提案人</th>
                <th>时间</th>
                <th>用户评价</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            { 
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Result1.Proposal1.Title)</td>
                    <td>@Html.DisplayFor(modelItem => item.Result1.Proposal1.ProposalType.Type)</td>
                    <td>@Html.DisplayFor(modelItem => item.Result1.Proposal1.User.UserName)</td>
                    <td>@Html.DisplayFor(modelItem => item.Date)</td>
                    <td>@Html.DisplayFor(modelItem => item.ProposerOpinion)</td>

                </tr>
        }
        </tbody>
    </table>

</body>
</html>